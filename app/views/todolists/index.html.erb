<h1>投稿一覧</h1>

<!--@lists.eachと記述することで、Listレコードの数だけ繰り返し処理する-->
<% @lists.each do |list| %>
  <p>タイトル</p>

  <span>
    <!--リンクなしでタイトルを表示する場合<%=list.title%>-->
    <!--詳細ページのリンクあり・URL<%=link_to list.title,"/todolist/#{list.id}"%>-->
    <!--詳細ページのリンクあり・名前付きルート-->
    <%= link_to list.title, todolist_path(list.id)%>
  </span>
<%end%>
